# Runtime dependencies for Nexus Liveness.
# Versions pinned for reproducibility; bump via Dependabot.

numpy>=2.2.6,<3
scipy>=1.15.3
opencv-python>=5.0.0.93
mediapipe>=0.10.35
matplotlib>=3.10.9

# API extras (install with `pip install -e ".[api]"`)
fastapi>=0.139.2
uvicorn[standard]>=0.51.0
pyjwt>=2.13.0
python-multipart>=0.0.32
